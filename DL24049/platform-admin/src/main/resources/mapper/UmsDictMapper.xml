<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.platform.com.auth.mapper.UmsDictMapper">

    <select id="selectByDictCode" parameterType="string" resultType="umsDictEntity">
        select * from  ums_dict where dict_code = #{dictCode}
    </select>
</mapper>
